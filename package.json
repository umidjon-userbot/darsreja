{
  "name": "smart-schedule-builder",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "Serversiz, constraint-based dars jadvali generatori (GitHub Pages)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:single": "vite build --mode single",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "qrcode-generator": "^1.4.4",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.0",
    "vitest": "^2.1.0"
  }
}
